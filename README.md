#KNOWLEDGE-LEARNING

Make sure you have the Symfony CLI installed on your machine.

For email delivery, note that GitHub will block the git push -u origin main command because the API key is missing. You’ll find the key in the provided PDF—add it to your .env file to enable email sending. Finally, ensure MySQL is properly configured.